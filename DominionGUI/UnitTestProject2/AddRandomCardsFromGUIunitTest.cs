using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using DominionGUI;

namespace UnitTestProject2
{
    [TestClass]
    public class AddRandomCardsFromGUIunitTest
    {
        /*[TestMethod]
        public void TestIfCorrectNumberOfCardsAreAddedToGameBoard()
        {
            SelectNumPlayers TEMP = new SelectNumPlayers(); //TODO fix this
            for (int i = 0; i < 100; i++)
            {
                SelectNumPlayers.INSTANCE.addRandomCards();
                Assert.AreEqual(10, SelectNumPlayers.INSTANCE.board.cards.Count);
            }
        }*/
    }
}
